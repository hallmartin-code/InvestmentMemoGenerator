ANTHROPIC_API_KEY=sk-...

# Email notifications (Resend). Without RESEND_API_KEY, generation still works
# and the notification is skipped.
RESEND_API_KEY=re_...
MEMO_EMAIL_TO=Info@tencapital.group
# Must be on a domain verified at https://resend.com/domains.
MEMO_EMAIL_FROM=TEN Capital Memo Generator <memos@tencapital.group>
